# Point the live test suite (npm run test:live) at an install.
EXPERTS_BASE_URL=http://localhost:5003
EXPERTS_TEST_KEY=sk-...
# Optional: an expert uid to exercise. Defaults to the first guest-visible one.
EXPERTS_TEST_EXPERT=
